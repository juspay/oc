name: AI
version: 1.0.0
description: Nix flake for AI coding agents
author: Sridhar Ratnakumar
# Lock file is gitignored because it breaks with multiple targets
# (https://github.com/microsoft/apm/issues/529). Run `just agent::update`
# to pull the latest upstream versions and re-vendor.
dependencies:
  apm:
  - juspay/skills
  - anthropics/skills/skills/frontend-design
  mcp: []
scripts: {}
